server:
    port: 8090

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/testDbDEA?createDatabaseIfNotExist=true
        username: root
        password: 11111111
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: update
        properties:
          hibernate:
             dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: true
